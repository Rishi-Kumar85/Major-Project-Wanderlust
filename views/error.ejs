<% layout("layouts/boilerplate") %>
<div class="row mt-3">
<div class="alert alert-danger col-6 offset-3" role="alert">
    <svg class="bi flex-shrink-0 me-2" width="24" height="24" role="img" aria-label="Danger:"><use xlink:href="#exclamation-triangle-fill"/></svg>
    <h4 class="alert-heading text-center">Oh no! An Error Occurred!</h4>
    <p class="text-center">🔥Error: <%= err.status %> - <%= err.message %></p>
    <hr>
    <p class="mb-0 text-center">Please try again later or contact support if the issue persists.</p>
</div>
</div>